# ProGuard / R8 rules for BeCalm Android.
# Populated incrementally as feature SPs are merged.
# Do not add speculative rules — add only when a build warning or runtime crash
# proves they are needed.
