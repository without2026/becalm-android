<?xml version="1.0" encoding="utf-8"?>
<!--
    Network security config.
    - All traffic is HTTPS by default (no cleartext allowed globally).
    - Cleartext exceptions for localhost (127.0.0.1) and the standard Android
      emulator host alias (10.0.2.2) to allow development against a local
      Railway/sidecar backend without a TLS certificate.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Android emulator host alias maps to developer machine loopback -->
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
