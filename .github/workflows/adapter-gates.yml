# Adapter dispatcher: routes deterministic gates to the correct platform adapter.
# Called by ci-review.yml with the adapter name from platform.yml.

name: Adapter Gates Dispatcher

on:
  workflow_call:
    inputs:
      adapter:
        description: "Adapter name from platform.yml (electron, android, etc.)"
        required: true
        type: string

jobs:
  android-gates:
    if: inputs.adapter == 'android'
    uses: ./.github/workflows/android-gates.yml
    secrets: inherit

  unsupported-adapter:
    if: inputs.adapter != 'android'
    runs-on: ubuntu-latest
    steps:
      - name: Fail unsupported adapter
        run: |
          echo "::error::No deterministic gate workflow is configured for adapter=${{ inputs.adapter }}"
          exit 1

  gate-result:
    runs-on: ubuntu-latest
    if: always()
    needs: [android-gates, unsupported-adapter]
    steps:
      - name: Verify at least one adapter ran
        run: |
          A="${{ needs.android-gates.result }}"
          U="${{ needs.unsupported-adapter.result }}"
          if [ "$A" = "success" ]; then
            echo "[PASS] Adapter gates completed"
          elif [ "$A" = "skipped" ] && [ "$U" = "success" ]; then
            echo "::error::No adapter matched: ${{ inputs.adapter }}"
            exit 1
          else
            echo "::error::Adapter gate failed (android=$A, unsupported=$U)"
            exit 1
          fi
