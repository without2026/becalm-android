source: email
folder: {folder}
default_direction: {default_direction}
user.phone_e164_self: {phone_e164_self}
user.display_name_override: {display_name_override}
rules:
- Extract commitments only from the primary message body, NOT from quoted_text.
- default_direction is a HINT; override from body evidence when clear.
- Preserve override evidence as `quote` field.
- Return a JSON array of CommitmentDraftDto: {direction, text, quote, person_ref, due_at, due_hint, due_is_approximate, confidence}.
