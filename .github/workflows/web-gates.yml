# Stub: this repo's adapter is 'android'. Web adapter is not configured.
name: Web Gates (stub)

on:
  workflow_call:

jobs:
  unsupported:
    runs-on: ubuntu-latest
    steps:
      - name: Adapter not configured
        run: |
          echo "::error::web adapter is not configured in this repo (platform.yml adapter=android)"
          exit 1
